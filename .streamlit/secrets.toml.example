# ===== Streamlit Secrets — salin file ini ke dashboard Community Cloud =====
# Lokal: taruh di .streamlit/secrets.toml (JANGAN commit — sudah di-gitignore)
# Cloud: Settings -> Secrets -> paste isi file ini
#
# PENTING: semua key harus di ROOT LEVEL (tanpa [section]) supaya otomatis
# jadi environment variable juga (cara baca paling kompatibel).

# LLM API — Groq (OpenAI-compatible). Model default: openai/gpt-oss-120b
# Ambil key di https://console.groq.com/keys lalu ganti nilainya di bawah.
GROQ_API_KEY="ISI_GROQ_API_KEY_DISINI"

# PostgreSQL (Supabase) — memory chat
DATABASE_URL = "postgresql://postgres.gpgqclbnriwirvaovxmb:ISI_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require"

# Google Sheet knowledge base
SHEET_ID = "1w3QAG-GWWHP3cg-4uIMPj41x-W3ZyiMfi3IS-JC4VbQ"
